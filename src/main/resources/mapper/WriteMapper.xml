<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.greatgump.crm.mapper.WriteMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.greatgump.crm.entity.Write">
        <id column="id" property="id" />
        <result column="odd_numbers" property="oddNumbers" />
        <result column="department" property="department" />
        <result column="reimbursement_amount" property="reimbursementAmount" />
        <result column="reimburser" property="reimburser" />
        <result column="approval_status" property="approvalStatus" />
        <result column="delivery_time" property="deliveryTime" />
        <result column="remark" property="remark" />
        <result column="is_delete" property="isDelete" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, odd_numbers, department, reimbursement_amount, reimburser, approval_status, delivery_time, remark, is_delete
    </sql>

</mapper>
